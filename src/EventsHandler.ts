import {Renderer} from "./Renderer.ts";


enum Event {
    mousemove,
    mouseover
}

class EventHandler {
    renderer: Renderer;

    constructor(renderer: Renderer) {
        this.renderer = renderer;
    }



}